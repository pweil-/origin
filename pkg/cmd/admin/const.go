package admin

const (
	//FlagDryRun is a const to represent the dry-run switch
	FlagDryRun = "dry-run"

	//FlagImages is a const to represent the images switch
	FlagImages = "images"

	//FlagImagesPullSecret is a const to represetn the images-pull-secret switch
	FlagImagesPullSecret = "images-pull-secret"
)
